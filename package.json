{
  "name": "ghardaia-delivery-app",
  "version": "1.0.0",
  "description": "Root package for Ghardaia Delivery Service",
  "scripts": {
    "install-all": "npm install && cd server && npm install && cd ../client && npm install",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  },
  "dependencies": {}
}
